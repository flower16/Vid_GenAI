# ---- Core RAG stack ----
# Pinned to the langchain 0.2.x line: RAGAS (below) is NOT yet compatible with
# langchain-community 0.3+/1.x, which removed chat_models.vertexai that RAGAS
# imports at load time. Keep these versions aligned.
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25
langchain-experimental==0.0.65      # SemanticChunker
langchain-text-splitters==0.2.4
openai>=1.40,<2.0

# ---- Vector store ----
faiss-cpu>=1.8.0
chromadb>=0.5.0                      # optional alternative store

# ---- Embeddings / reranking ----
sentence-transformers>=3.0.0         # BAAI/bge-reranker-large cross-encoder
torch>=2.2.0

# ---- Document loading ----
pypdf>=4.2.0
python-docx>=1.1.0
docx2txt>=0.8
unstructured>=0.14.0

# ---- Evaluation ----
ragas==0.1.21
datasets>=2.19.0,<3.0.0

# ---- Data / app ----
pandas>=2.2.0
numpy>=1.26.0,<2.0.0
streamlit>=1.35.0
python-dotenv>=1.0.1
tqdm>=4.66.0
matplotlib>=3.8.0
